# Model Registry

The model registry tracks trained strategy artifacts by metadata and artifact hash. No trained model is packaged in the release ZIP and no model defaults to live execution.

Default deployment posture is manual-review-only or not deployed. A registered model can help generate strategy signal candidates, but those candidates still require manual review and all existing risk/live gates.
