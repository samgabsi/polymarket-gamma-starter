# Raw Snapshots

Raw snapshots are runtime records captured from explicit local/import ingestion jobs. The release ZIP does not include generated snapshots.

Each snapshot stores metadata, hashes, source ID, ingestion job ID, snapshot type, record count, time range, market/token hints, warnings, blockers, and a local runtime storage reference.
