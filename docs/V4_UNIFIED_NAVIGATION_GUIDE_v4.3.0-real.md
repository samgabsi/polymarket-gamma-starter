# V4 Unified Navigation Guide - v4.3.0-real

Polymarket OP Console v4.3.0-real flattens the user experience across root, v2 live controls, v3 Operator OS, AI Copilot, platform diagnostics, cockpit, workspace, tasks, datasets, freshness, simulation, analytics, and settings.

## Recommended start

Use `/v3` or `/operator-os` as the primary operator workflow starting point. Use `/` for the broader product home and `/system-map` for route discovery.

## Friendly aliases

- `/ai` -> `/v3/ai`
- `/platform` -> `/v3/platform`
- `/cockpit` -> `/v3/cockpit`
- `/workspace` -> `/v3/workspace`
- `/tasks` -> `/v3/tasks`
- `/live` and `/live-controls` -> `/v2-live`
- `/operator-os` -> `/v3`
- `/routes` -> `/system-map`

## Safety

Aliases only redirect. They do not bypass authentication, warnings, blockers, approval requirements, typed confirmations, or backend safety gates.
