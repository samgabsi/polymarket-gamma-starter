# Normalized Market Data

Normalization converts raw snapshot rows into deterministic records for training and labeling.

Normalized records include source ID, snapshot ID, market ID, condition ID, token ID, timestamp, record type, schema version, content hash, warnings, and blockers.

Normalization is offline and does not trade.
