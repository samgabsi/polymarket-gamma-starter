# Labeling Workbench

The labeling workbench creates labels for supervised training and backtesting. Labels can be wrong and require operator review.

Supported label families include price/probability movement, spread/liquidity acceptability, execution-quality acceptability, historical approval/signal labels, and manual custom labels.

Generated labels are runtime state and are excluded from release ZIPs.
