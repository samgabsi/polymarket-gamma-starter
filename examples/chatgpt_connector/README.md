# ChatGPT Connector Blueprint Example

This directory contains non-executable blueprint stubs for a future read-only ChatGPT/MCP connector.

The connector is disabled by default. Any future implementation must require authentication, expose read-only summary/draft tools only, redact inputs, reject live mutation capabilities, and never expose secrets.

