# Connector Security Notes

This blueprint is not a live server.

Before any future connector is enabled, require authentication, keep all tools read-only, deny live mutation capability names, redact inputs and outputs, store no secrets, and keep model/tool output as human-review drafts only.

